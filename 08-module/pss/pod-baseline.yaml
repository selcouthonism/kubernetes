apiVersion: v1
kind: Pod
metadata:
  name: pod-nginx-baseline
spec:
  containers:
  - name: nginx-container
    image: nginx:1.27.0
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
      requests:
        memory: "64Mi"
        cpu: "250m"
    ports:
    - containerPort: 80