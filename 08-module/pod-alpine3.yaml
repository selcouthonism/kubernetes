apiVersion: v1
kind: Pod
metadata:
  name: alpine-curl-pod-3
  labels:
    app: alpine-curl-3
    tier: backend
spec:
  containers:
  - name: alpine-curl-container
    image: alpine:3.20
    command: ["/bin/sh", "-c", "apk update && apk add curl && sleep infinity"]