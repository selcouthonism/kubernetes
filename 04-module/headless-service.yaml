apiVersion: v1
kind: Service
metadata:
  name: color-headless-svc
spec:
  clusterIP: None  # Headless service
  selector:
    app: color-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
